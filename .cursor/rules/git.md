# Git Rules
- NEVER create new branches. Commit to current branch.
- NEVER push without being explicitly told.
- NEVER force push.
- After every commit, run git log --oneline -1 and show the hash.
- One logical change per commit.
